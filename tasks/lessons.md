# Lessons Learned

<!-- Self-learning file: every correction you give Claude gets written here as a rule. -->
<!-- Read at session start, applied before touching any code. -->

## Rules
- Do NOT add "Co-Authored-By: Claude" to git commit messages
- localStorage caching of transaction data is disabled by default (privacy concern)
- Always run `/session-end` before closing a session
